const STUDY_DATA = {
  "Portronics-Highlighter-Set": {
    brand: "Portronics",
    title: "Portronics Highlighter Set",
    rating: "4.5",
    ratingsCount: "184 ratings",
    price: 199,
    originalPrice: 399,
    discount: "50% off",
    images: [
      "https://images.unsplash.com/photo-1583485088034-697b5bc54ccd?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1513542789411-b6a5d4f31634?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1456735190827-d1262f71b8a3?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1484480974693-6ca0a78fb36b?auto=format&fit=crop&w=800&q=80"
    ],
    colorOptions: [
      { name: "Multi-Color Pack", img: "https://images.unsplash.com/photo-1583485088034-697b5bc54ccd?auto=format&fit=crop&w=800&q=80" }
    ],
    sizes: ["Pack of 3", "Pack of 6", "Pack of 12"],
    sizeLabel: "Select Pack Size",
    highlights: {
      "Brand": "Portronics",
      "Ink Type": "Fluorescent Water-Based",
      "Line Size": "1mm - 4mm Chisel Tip",
      "Colors": "Pink, Yellow, Orange, Green, Blue, Purple",
      "Pack Size": "6 Highlighters Included",
      "Non-Toxic": "Yes (ASTM D-4236 Approved)",
      "Usage": "Note-Taking / Textbook Marking"
    },
    extraSections: {
      "Material & Ink Details": "Our highlighters are filled with premium, ultra-bright, non-toxic, and quick-drying water-based ink that doesn't bleed through standard textbook or journal paper.",
      "Ergonomic Design": "Features a comfortable flat barrel with a textured grip, allowing you to highlight smoothly without cramping your hands during long campus study sessions.",
      "Student Utility": "Essential for organizing lecture notes, active recall study methods, marking key definitions, and preparing exam flashcards."
    },
    seller: "Portronics Official Store"
  },
  "Kangaro-Executive-Diary": {
    brand: "Kangaro",
    title: "Kangaro Executive Diary",
    rating: "4.7",
    ratingsCount: "340 ratings",
    price: 249,
    originalPrice: 499,
    discount: "50% off",
    images: [
      "https://images.unsplash.com/photo-1544816155-12df9643f363?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1586075010923-2dd4570fb338?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1531346878377-a5be20888e57?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1512820790803-83ca734da794?auto=format&fit=crop&w=800&q=80"
    ],
    colorOptions: [
      { name: "Classic Navy", img: "https://images.unsplash.com/photo-1544816155-12df9643f363?auto=format&fit=crop&w=800&q=80" },
      { name: "Stealth Black", img: "https://images.unsplash.com/photo-1586075010923-2dd4570fb338?auto=format&fit=crop&w=800&q=80" }
    ],
    sizes: ["A5 Hardcover", "A6 Pocket", "B5 Journal"],
    sizeLabel: "Select Diary Size",
    highlights: {
      "Brand": "Kangaro",
      "Page Count": "192 Pages (Double-Sided)",
      "Paper Density": "80 GSM Acid-Free",
      "Cover Material": "Premium PU Leather Hardcover",
      "Layout": "Ruled (Single Line)",
      "Binding": "Stitched Lay-Flat Binding",
      "Add-ons": "Ribbon Bookmark & Elastic Closure"
    },
    extraSections: {
      "Paper Quality": "Enjoy an exceptionally smooth writing experience with 80 GSM acid-free ivory pages that resist feathering and fountain pen ink bleeding.",
      "Durability & Style": "Wrapped in luxurious water-resistant PU leather with an elegant embossed logo, built to withstand daily backpack commutes.",
      "Student Usage": "Perfect for lecture notes, daily schedules, project plans, personal journaling, and college assignment tracking."
    },
    seller: "Kangaro India Stationery"
  },
  "Portronics-RGB-DeskLamp": {
    brand: "Portronics",
    title: "Portronics RGB Desk Lamp",
    rating: "4.4",
    ratingsCount: "215 ratings",
    price: 899,
    originalPrice: 1799,
    discount: "50% off",
    images: [
      "https://images.unsplash.com/photo-1507473885765-e6ed057f782c?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1534073828943-f801091bb18c?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1513506003901-1e6a229e2d15?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1517991104123-1d56a6e81ed9?auto=format&fit=crop&w=800&q=80"
    ],
    colorOptions: [
      { name: "Stealth Black", img: "https://images.unsplash.com/photo-1507473885765-e6ed057f782c?auto=format&fit=crop&w=800&q=80" },
      { name: "Sleek White", img: "https://images.unsplash.com/photo-1534073828943-f801091bb18c?auto=format&fit=crop&w=800&q=80" }
    ],
    sizes: ["Eco Dim", "Stepless RGB", "Night Light Mode"],
    sizeLabel: "Select Lighting Mode",
    highlights: {
      "Brand": "Portronics",
      "Power Source": "USB Rechargeable (1200mAh)",
      "Lighting Modes": "3 Color temps + Multi-color RGB Base",
      "Control": "Touch Sensitive Switch",
      "Brightness": "Stepless Dimming (up to 400 Lumens)",
      "Battery Life": "Up to 8 hours on single charge",
      "Neck Type": "360-degree Flexible Gooseneck"
    },
    extraSections: {
      "RGB Ambient Lighting": "Features a gorgeous multi-color dynamic RGB base that adds a modern gaming/cozy aesthetic to your hostel study desk.",
      "Eye Protection Features": "Equipped with flicker-free, low-blue diffused LED panels that mimic natural lighting, ensuring comfort during all-night exams.",
      "Smart Touch Controls": "Tap to toggle warm/cool lights, and long-press to smoothly dim the light to your preferred level."
    },
    seller: "Portronics Official Store"
  },
  "Philips-RGB-DeskLamp": {
    brand: "Philips",
    title: "Philips RGB Desk Lamp",
    rating: "4.6",
    ratingsCount: "512 ratings",
    price: 1299,
    originalPrice: 2499,
    discount: "48% off",
    images: [
      "https://images.unsplash.com/photo-1533090161767-e6ffed986c88?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1542728928-1413d1894ed1?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1507473885765-e6ed057f782c?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1513506003901-1e6a229e2d15?auto=format&fit=crop&w=800&q=80"
    ],
    colorOptions: [
      { name: "Pearl White", img: "https://images.unsplash.com/photo-1533090161767-e6ffed986c88?auto=format&fit=crop&w=800&q=80" }
    ],
    sizes: ["Relax Mode", "Study Mode", "Work Mode"],
    sizeLabel: "Lighting Preset",
    highlights: {
      "Brand": "Philips",
      "LED Lifetime": "Up to 25,000 Hours",
      "Max Luminance": "500 Lumens",
      "Special Mode": "Integrateable RGB Base Ambient Light",
      "Controls": "Smart Touch Slider Panel",
      "Anti-Glare": "Yes (Honey-Comb Micro Lens Optics)",
      "Power": "12W AC Adapter Included"
    },
    extraSections: {
      "Advanced Optics": "Utilizes Philips' proprietary Honey-Comb optic lenses to spread light evenly, reducing glare and eye strain by 70%.",
      "RGB Ambilight Base": "Independent customizable base ring lets you set soft color gradients to create a soothing room ambience at night.",
      "Study & Focus Modes": "Optimize concentration with cold daylight temperatures (6000K) or switch to warm yellow (3000K) for casual reading."
    },
    seller: "Philips Lighting India"
  },
  "Wipro-RGB-DeskLamp": {
    brand: "Wipro",
    title: "Wipro RGB Desk Lamp – Navy",
    rating: "4.5",
    ratingsCount: "289 ratings",
    price: 999,
    originalPrice: 1999,
    discount: "50% off",
    images: [
      "https://images.unsplash.com/photo-1513506003901-1e6a229e2d15?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1507473885765-e6ed057f782c?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1542728928-1413d1894ed1?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1533090161767-e6ffed986c88?auto=format&fit=crop&w=800&q=80"
    ],
    colorOptions: [
      { name: "Navy Blue", img: "https://images.unsplash.com/photo-1513506003901-1e6a229e2d15?auto=format&fit=crop&w=800&q=80" },
      { name: "Carbon Grey", img: "https://images.unsplash.com/photo-1507473885765-e6ed057f782c?auto=format&fit=crop&w=800&q=80" }
    ],
    sizes: ["3000K Warm Glow", "4000K Neutral Day", "6500K Cool White"],
    sizeLabel: "Color Temperature",
    highlights: {
      "Brand": "Wipro",
      "Color": "Navy Blue Matte Finish",
      "Interface": "Dynamic Touch Slider",
      "USB Charging Port": "Yes (5V/1A output to charge phone)",
      "Luminous Flux": "450 Lumens",
      "Base Light": "7-color breathing RGB LED Base",
      "Adjustability": "Dual-Axis Foldable Arm"
    },
    extraSections: {
      "Phone Charging Convenience": "Features a built-in USB output port, allowing you to charge your phone or earbuds directly from the lamp without running extra wires.",
      "Dual-Axis Ergonomics": "Adjust the light angle with high precision using the 180-degree folding arm and 90-degree base rotating axis.",
      "Gaming Setup Fit": "The deep matte Navy Blue coating combined with the breathing base light matches any premium study or gaming setup."
    },
    seller: "Wipro Consumer Care Store"
  },
  "Mi-LED-DeskLamp": {
    brand: "Mi",
    title: "Mi LED Desk Lamp – White",
    rating: "4.8",
    ratingsCount: "780 ratings",
    price: 1499,
    originalPrice: 2999,
    discount: "50% off",
    images: [
      "https://images.unsplash.com/photo-1534073828943-f801091bb18c?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1533090161767-e6ffed986c88?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1507473885765-e6ed057f782c?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1542728928-1413d1894ed1?auto=format&fit=crop&w=800&q=80"
    ],
    colorOptions: [
      { name: "Minimal White", img: "https://images.unsplash.com/photo-1534073828943-f801091bb18c?auto=format&fit=crop&w=800&q=80" }
    ],
    sizes: ["Reading Mode", "Computer Mode", "Child Mode", "Focus Mode"],
    sizeLabel: "Smart Scene Modes",
    highlights: {
      "Brand": "Xiaomi (Mi)",
      "Design": "Red Dot Award-Winning Minimal Design",
      "Color Temperature": "2700K - 6500K Stepless Adjustment",
      "App Support": "Mi Home / Apple HomeKit Compatible",
      "Protection": "Zero Visible Flicker & Low Blue Light",
      "Knob Control": "Single Multifunctional Rotary Knob",
      "Power": "9W Low Energy Consumption"
    },
    extraSections: {
      "Smart Control Options": "Control the lamp with the rotary dial knob on the base (click to turn on/off, spin to adjust brightness, press and spin to change color temp) or sync with Mi Home app.",
      "Award Winning Design": "Designed with a fully metallic slim hinge mechanism that folds flat when not in use, fitting perfectly into any modern space.",
      "Professional Eye-Care": "Meets professional lighting certification standards, providing smooth light coverage that prevents optical fatigue."
    },
    seller: "Xiaomi India Retail Store"
  },
  "Portronics-LED-DeskLamp": {
    brand: "Portronics",
    title: "Portronics LED Desk Lamp",
    rating: "4.3",
    ratingsCount: "167 ratings",
    price: 599,
    originalPrice: 1199,
    discount: "50% off",
    images: [
      "https://images.unsplash.com/photo-1542728928-1413d1894ed1?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1507473885765-e6ed057f782c?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1534073828943-f801091bb18c?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1533090161767-e6ffed986c88?auto=format&fit=crop&w=800&q=80"
    ],
    colorOptions: [
      { name: "Alabaster White", img: "https://images.unsplash.com/photo-1542728928-1413d1894ed1?auto=format&fit=crop&w=800&q=80" }
    ],
    sizes: ["30% Eco Dim", "60% Study Dim", "100% Full Power"],
    sizeLabel: "Select Intensity",
    highlights: {
      "Brand": "Portronics",
      "Neck": "Flexible 360-degree Gooseneck",
      "Interface": "One-Touch Sensor Control",
      "Mounting": "Heavy Stable Base with Organizer Slots",
      "Power Source": "USB Cable Included (Works with Power Banks)",
      "Weight": "Ultra Portable (approx. 280g)",
      "No-Glare Filter": "Yes (Opal Acrylic Diffuser)"
    },
    extraSections: {
      "Built-in Desk Organizer": "Designed with standard pen holder slots and a phone resting stand carved directly into the base for space conservation.",
      "Gooseneck Adjustability": "The flexible silicone neck allows you to position the light at any extreme angle, perfect for bunk bed reading.",
      "USB Powered Portability": "Can be powered by a laptop USB port or any standard power bank, keeping it active during hostel power outages."
    },
    seller: "Portronics Official Store"
  },
  "Mi-ClipOn-StudyLamp": {
    brand: "Mi",
    title: "Mi Clip-On Study Lamp",
    rating: "4.6",
    ratingsCount: "420 ratings",
    price: 499,
    originalPrice: 999,
    discount: "50% off",
    images: [
      "https://images.unsplash.com/photo-1517991104123-1d56a6e81ed9?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1507473885765-e6ed057f782c?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1534073828943-f801091bb18c?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1533090161767-e6ffed986c88?auto=format&fit=crop&w=800&q=80"
    ],
    colorOptions: [
      { name: "Ice White", img: "https://images.unsplash.com/photo-1517991104123-1d56a6e81ed9?auto=format&fit=crop&w=800&q=80" }
    ],
    sizes: ["Low Ambience", "Medium Reading", "High Spotlight"],
    sizeLabel: "Select Mode",
    highlights: {
      "Brand": "Xiaomi (Mi)",
      "Clamp Size": "Opens up to 4.5 cm with rubber padding",
      "Battery Life": "Up to 40 Hours of low light runtime",
      "Power Source": "Rechargeable Type-C (1500mAh)",
      "Lighting Panel": "Ring-shaped LED for uniform light",
      "Portability": "Clip and stand 2-in-1 dual usage"
    },
    extraSections: {
      "Rugged Clamp Mechanism": "Fitted with professional high-tension steel springs and rubberized grip panels to clamp securely to bed rails, study tables, or bookshelves.",
      "Ring LED Uniformity": "Circular lighting array reduces shadows and light hotspots, mimicking hospital surgical lighting setups for eye comfort.",
      "Rechargeable Type-C Convenience": "Includes a fast Type-C charging port, providing massive standalone battery life that is perfect for late-night exam prep."
    },
    seller: "Xiaomi India Retail Store"
  }
};

function getActiveProduct() {
  const filename = window.location.pathname.split('/').pop() || 'Portronics-Highlighter-Set.html';
  const slug = filename.replace('.html', '');
  return STUDY_DATA[slug] || STUDY_DATA["Portronics-Highlighter-Set"];
}

// Global active selection states
let selectedColor = "";
let selectedSize = "";
let currentAction = null;

function renderProductDetail() {
  const prod = getActiveProduct();
  if (!prod) return;

  // 1. Update document head title
  document.title = `${prod.title} | HostelMart`;

  // 2. Breadcrumbs Setup (Study specific)
  const navEl = document.querySelector('main > nav');
  if (navEl) {
    navEl.innerHTML = `
      <a href="index.html" class="hover:text-[#3E2723]">Home</a>
      <ion-icon name="chevron-forward-outline"></ion-icon>
      <a href="StudyEssentials.html" class="hover:text-[#3E2723]">Study Essentials</a>
      <ion-icon name="chevron-forward-outline"></ion-icon>
      <a href="StudyEssentials.html" class="hover:text-[#3E2723]">${prod.brand}</a>
      <ion-icon name="chevron-forward-outline"></ion-icon>
      <span class="text-[#3E2723] font-bold">${prod.title}</span>
    `;
  }

  // 3. Grid Images Setup
  for (let i = 0; i < 4; i++) {
    const imgEl = document.getElementById(`main-img-${i + 1}`);
    if (imgEl) {
      imgEl.src = prod.images[i] || prod.images[0];
      imgEl.alt = `${prod.title} View ${i + 1}`;
    }
  }

  // 4. Product Details Headers
  const brandEl = document.getElementById('product-brand');
  const titleEl = document.getElementById('product-title');
  if (brandEl) brandEl.textContent = prod.brand;
  if (titleEl) titleEl.textContent = prod.title;

  // 5. Ratings & Reviews
  const ratingEl = document.getElementById('product-rating');
  const ratingCountEl = document.getElementById('product-rating-count');
  if (ratingEl) ratingEl.textContent = prod.rating;
  if (ratingCountEl) ratingCountEl.textContent = prod.ratingsCount;

  // 6. Pricing Section
  const priceEl = document.getElementById('product-price');
  const originalPriceEl = document.getElementById('product-original-price');
  const discountEl = document.getElementById('product-discount');
  const buyBtnText = document.getElementById('buy-btn-price-text');
  
  if (priceEl) priceEl.textContent = `₹${prod.price}`;
  if (originalPriceEl) originalPriceEl.textContent = `₹${prod.originalPrice}`;
  if (discountEl) discountEl.textContent = prod.discount;
  if (buyBtnText) buyBtnText.textContent = `Buy at ₹${prod.price}`;

  const dealEl = document.getElementById('deal-saving-text');
  if (dealEl) {
    const dealPrice = Math.floor(prod.price * 0.90); // 10% off for Student Deal
    dealEl.textContent = `Buy at ₹${dealPrice}`;
  }

  // Update size label in UI
  const labelEl = document.querySelector('p.text-\\[10px\\].font-bold.text-gray-400.uppercase.tracking-widest:nth-of-type(2)');
  if (labelEl && prod.sizeLabel) {
    labelEl.textContent = prod.sizeLabel;
  }

  // 7. Variants Color Builder
  const variantsContainer = document.getElementById('variants-container');
  if (variantsContainer) {
    selectedColor = prod.colorOptions[0].name;
    document.getElementById('color-label').textContent = selectedColor;

    variantsContainer.innerHTML = prod.colorOptions.map((opt, idx) => `
      <button onclick="updateVariant('${opt.name}')" class="variant-btn ${idx === 0 ? 'active' : ''} w-16 h-20 rounded-xl overflow-hidden border-2 border-transparent transition-all shadow-md">
        <img src="${opt.img}" class="w-full h-full object-cover">
      </button>
    `).join('');
  }

  // 8. Size/Mode Selector chips Builder
  const sizesContainer = document.getElementById('sizes-container');
  if (sizesContainer) {
    sizesContainer.innerHTML = prod.sizes.map(sz => `
      <button onclick="selectSize(this)" class="size-btn px-4 h-12 rounded-xl border border-[#D7CCC8] flex items-center justify-center text-xs font-black transition-all hover:border-[#3E2723]">${sz}</button>
    `).join('');
  }

  // 9. Highlights Specifications Builder
  const highlightsContainer = document.getElementById('highlights-container');
  if (highlightsContainer) {
    highlightsContainer.innerHTML = Object.entries(prod.highlights).map(([key, val]) => `
      <div class="space-y-1">
        <p class="text-[10px] font-bold text-gray-400 uppercase tracking-widest">${key}</p>
        <p class="text-xs font-black">${val}</p>
      </div>
    `).join('');
  }

  // 9.1 Dynamic Extra Sections Builder
  const extraContainer = document.getElementById('extra-sections-container');
  if (extraContainer) {
    if (prod.extraSections) {
      extraContainer.classList.remove('hidden');
      extraContainer.innerHTML = Object.entries(prod.extraSections).map(([key, val]) => `
        <div class="space-y-2 pt-6 border-t border-[#D7CCC8]/30">
          <p class="text-[10px] font-bold text-gray-400 uppercase tracking-widest">${key}</p>
          <p class="text-xs font-semibold leading-relaxed text-gray-600">${val}</p>
        </div>
      `).join('');
    } else {
      extraContainer.classList.add('hidden');
      extraContainer.innerHTML = "";
    }
  }

  // 10. Seller & Return Details
  const sellerEl = document.getElementById('seller-name');
  if (sellerEl) sellerEl.textContent = prod.seller;

  // 11. Select Variant Modal Sync
  const modalImg = document.querySelector('.variant-modal-content img');
  const modalBrand = document.querySelector('.variant-modal-content h4');
  const modalTitle = document.querySelector('.variant-modal-content p');
  const modalOriginal = document.querySelector('.variant-modal-content span.text-xs');
  const modalPrice = document.querySelector('.variant-modal-content span.text-lg');
  const modalSizesContainer = document.querySelector('.variant-modal-content .justify-between');

  if (modalImg) modalImg.src = prod.images[0];
  if (modalBrand) modalBrand.textContent = prod.brand;
  if (modalTitle) modalTitle.textContent = prod.title;
  if (modalOriginal) modalOriginal.textContent = `₹${prod.originalPrice}`;
  if (modalPrice) modalPrice.textContent = `₹${prod.price}`;
  
  if (modalSizesContainer) {
    modalSizesContainer.innerHTML = prod.sizes.map(sz => `
      <button onclick="selectSizeModal(this)" class="size-btn-modal px-3 py-2 rounded-xl border border-gray-200 flex items-center justify-center text-xs font-black transition-all hover:border-[#3E2723]">${sz}</button>
    `).join('');
  }

  // Dynamic Date calculation (+2 days for superfast express student shipping)
  const deliveryDate = new Date();
  deliveryDate.setDate(deliveryDate.getDate() + 2);
  const dateOptions = { day: 'numeric', month: 'short', weekday: 'short' };
  const deliveryDateStr = deliveryDate.toLocaleDateString('en-IN', dateOptions);
  
  const deliveryDateEl = document.getElementById('delivery-date-estimate');
  if (deliveryDateEl) deliveryDateEl.textContent = deliveryDateStr;

  // Sync added state
  const filename = window.location.pathname.split('/').pop() || 'Portronics-Highlighter-Set.html';
  const slug = filename.replace('.html', '');
  const cart = JSON.parse(localStorage.getItem('hostelmart_cart')) || [];
  const inCart = cart.some(item => item.slug === slug || item.id === slug);
  if (inCart) {
    updateCartButtonState();
  }
}

// User Action Handlers
window.selectSize = function(btn) {
  document.querySelectorAll('.size-btn').forEach(b => b.classList.remove('active', 'bg-[#3E2723]', 'text-white'));
  btn.classList.add('active', 'bg-[#3E2723]', 'text-white');
  selectedSize = btn.innerText;
};

window.updateVariant = function(color) {
  document.querySelectorAll('.variant-btn').forEach(b => b.classList.remove('active'));
  event.currentTarget.classList.add('active');
  selectedColor = color;
  document.getElementById('color-label').innerText = color;
  
  // Swap the main preview image based on variant
  const prod = getActiveProduct();
  const matched = prod.colorOptions.find(o => o.name === color);
  if (matched) {
    const mainImg = document.getElementById('main-img-1');
    if (mainImg) mainImg.src = matched.img;
  }
};

window.handleBuyNow = function() {
  if (!selectedSize) {
    currentAction = 'buy';
    openVariantModal();
  } else {
    const prod = getActiveProduct();
    const filename = window.location.pathname.split('/').pop() || 'Portronics-Highlighter-Set.html';
    const slug = filename.replace('.html', '');
    const data = {
      id: slug,
      slug: slug,
      img: prod.images[0],
      brand: prod.brand,
      title: prod.title,
      currentPrice: `₹${prod.price}`,
      originalPrice: `₹${prod.originalPrice}`,
      size: selectedSize,
      color: selectedColor,
      qty: 1
    };
    localStorage.setItem('checkoutItem', JSON.stringify(data));
    window.location.href = 'Checkout.html';
  }
};

window.handleAddToCart = function() {
  if (!selectedSize) {
    currentAction = 'cart';
    openVariantModal();
  } else {
    const prod = getActiveProduct();
    const filename = window.location.pathname.split('/').pop() || 'Portronics-Highlighter-Set.html';
    const slug = filename.replace('.html', '');
    const data = {
      id: slug,
      slug: slug,
      img: prod.images[0],
      brand: prod.brand,
      title: prod.title,
      currentPrice: `₹${prod.price}`,
      originalPrice: `₹${prod.originalPrice}`,
      size: selectedSize,
      color: selectedColor,
      qty: 1
    };
    if (window.addToCart) {
      window.addToCart(data);
    }
    updateCartButtonState();
  }
};

window.confirmVariantSelection = function() {
  const selectedSizeModal = document.querySelector('.size-btn-modal.active');
  if (!selectedSizeModal) return;
  
  selectedSize = selectedSizeModal.innerText;
  
  // Reflect size on main selectors
  document.querySelectorAll('.size-btn').forEach(b => {
    if (b.innerText === selectedSize) {
      b.classList.add('active', 'bg-[#3E2723]', 'text-white');
    } else {
      b.classList.remove('active', 'bg-[#3E2723]', 'text-white');
    }
  });

  const prod = getActiveProduct();
  const filename = window.location.pathname.split('/').pop() || 'Portronics-Highlighter-Set.html';
  const slug = filename.replace('.html', '');
  const data = {
    id: slug,
    slug: slug,
    img: prod.images[0],
    brand: prod.brand,
    title: prod.title,
    currentPrice: `₹${prod.price}`,
    originalPrice: `₹${prod.originalPrice}`,
    size: selectedSize,
    color: selectedColor,
    qty: 1
  };

  if (currentAction === 'buy') {
    localStorage.setItem('checkoutItem', JSON.stringify(data));
    window.location.href = 'Checkout.html';
  } else {
    if (window.addToCart) {
      window.addToCart(data);
    }
    updateCartButtonState();
    closeVariantModal();
  }
};

window.selectSizeModal = function(btn) {
  document.querySelectorAll('.size-btn-modal').forEach(b => b.classList.remove('active', 'bg-[#3E2723]', 'text-white'));
  btn.classList.add('active', 'bg-[#3E2723]', 'text-white');
  document.getElementById('modal-continue-btn').disabled = false;
};

window.updateCartButtonState = function() {
  const mainBtn = document.getElementById('add-to-cart-btn');
  if (mainBtn) {
    mainBtn.innerHTML = '<ion-icon name="bag-handle-outline" class="text-lg"></ion-icon> Go To Cart';
    mainBtn.onclick = () => window.location.href = 'Cart.html';
    mainBtn.classList.replace('bg-white', 'bg-[#FDFBF7]');
  }
};

window.openVariantModal = function() {
  const modal = document.getElementById('variant-modal');
  modal.classList.remove('hidden');
  document.body.style.overflow = 'hidden';
  setTimeout(() => {
    modal.classList.add('active');
  }, 10);
};

window.closeVariantModal = function() {
  const modal = document.getElementById('variant-modal');
  modal.classList.remove('active');
  setTimeout(() => {
    modal.classList.add('hidden');
    document.body.style.overflow = 'auto';
  }, 300);
};

// Premium Features: Hover Zoom Tracking
function initHoverZoom() {
  document.querySelectorAll('.image-zoom-container').forEach(container => {
    const img = container.querySelector('img');
    if (!img) return;

    container.addEventListener('mousemove', e => {
      const rect = container.getBoundingClientRect();
      const x = ((e.clientX - rect.left) / rect.width) * 100;
      const y = ((e.clientY - rect.top) / rect.height) * 100;
      img.style.transformOrigin = `${x}% ${y}%`;
      img.style.transform = 'scale(1.5)';
    });

    container.addEventListener('mouseleave', () => {
      img.style.transform = 'scale(1)';
      img.style.transformOrigin = 'center center';
    });
  });
}

// Lightbox modal with thumbnail switcher
function initLightbox() {
  const lightboxHTML = `
    <div id="lightbox-modal" class="fixed inset-0 z-[150] bg-black/95 backdrop-blur-md hidden flex items-center justify-center p-4">
      <button id="close-lightbox" class="absolute top-6 right-6 text-white text-3xl p-3 hover:bg-white/10 rounded-full transition-all">
        <ion-icon name="close-outline"></ion-icon>
      </button>
      
      <div class="flex flex-col md:flex-row items-center gap-8 max-w-5xl w-full h-[80vh] md:h-auto">
        <div class="flex md:flex-col gap-3 overflow-x-auto md:overflow-x-visible no-scrollbar max-w-full md:max-w-none flex-shrink-0" id="lightbox-thumbs">
        </div>
        <div class="flex-1 h-[50vh] md:h-[70vh] rounded-3xl overflow-hidden bg-black/40 border border-white/10 relative flex items-center justify-center">
          <img id="lightbox-main-img" src="" class="max-w-full max-h-full object-contain transition-all duration-300">
        </div>
      </div>
    </div>
  `;
  document.body.insertAdjacentHTML('beforeend', lightboxHTML);

  const modal = document.getElementById('lightbox-modal');
  const mainImg = document.getElementById('lightbox-main-img');
  const thumbsContainer = document.getElementById('lightbox-thumbs');
  const closeBtn = document.getElementById('close-lightbox');

  closeBtn.onclick = () => {
    modal.classList.add('hidden');
    document.body.style.overflow = 'auto';
  };

  modal.onclick = (e) => {
    if (e.target === modal) {
      modal.classList.add('hidden');
      document.body.style.overflow = 'auto';
    }
  };

  document.querySelectorAll('.image-zoom-container').forEach((gridContainer, idx) => {
    gridContainer.style.cursor = 'zoom-in';
    gridContainer.onclick = () => {
      const activeProd = getActiveProduct();
      if (!activeProd) return;
      
      thumbsContainer.innerHTML = activeProd.images.map((imgUrl, i) => `
        <div class="w-16 h-20 rounded-xl overflow-hidden cursor-pointer border-2 ${i === idx ? 'border-amber-400' : 'border-transparent'} transition-all shadow-md lightbox-thumb-item" onclick="switchLightboxImage(${i})">
          <img src="${imgUrl}" class="w-full h-full object-cover">
        </div>
      `).join('');
      
      mainImg.src = activeProd.images[idx];
      modal.classList.remove('hidden');
      document.body.style.overflow = 'hidden';
    };
  });
}

window.switchLightboxImage = function(idx) {
  const activeProd = getActiveProduct();
  if (!activeProd) return;
  
  const mainImg = document.getElementById('lightbox-main-img');
  mainImg.src = activeProd.images[idx];
  
  document.querySelectorAll('.lightbox-thumb-item').forEach((item, i) => {
    if (i === idx) {
      item.classList.add('border-amber-400');
      item.classList.remove('border-transparent');
    } else {
      item.classList.add('border-transparent');
      item.classList.remove('border-amber-400');
    }
  });
};

// Map GPS Address Modal Logic
let addressMap;
let addressMarker;

window.openAddressModal = function() {
  const modal = document.getElementById('address-modal');
  const container = document.getElementById('address-container');
  
  modal.classList.remove('hidden');
  document.body.style.overflow = 'hidden';

  setTimeout(() => {
    container.classList.remove('scale-95', 'opacity-0');
    container.classList.add('scale-100', 'opacity-100');
    initAddressMap();
  }, 10);
};

window.closeAddressModal = function() {
  const container = document.getElementById('address-container');
  
  container.classList.remove('scale-100', 'opacity-100');
  container.classList.add('scale-95', 'opacity-0');

  setTimeout(() => {
    document.getElementById('address-modal').classList.add('hidden');
    document.body.style.overflow = 'auto';
  }, 500);
};

window.initAddressMap = function() {
  if (addressMap) return;

  const defaultLoc = [12.9716, 77.5946];
  addressMap = L.map('address-map').setView(defaultLoc, 13);

  L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
    attribution: '&copy; OpenStreetMap contributors'
  }).addTo(addressMap);

  addressMarker = L.marker(defaultLoc, { draggable: true }).addTo(addressMap);

  addressMarker.on('dragend', function() {
    const position = addressMarker.getLatLng();
    reverseGeocode(position.lat, position.lng);
  });

  addressMap.on('click', function(e) {
    addressMarker.setLatLng(e.latlng);
    reverseGeocode(e.latlng.lat, e.latlng.lng);
  });
};

window.useGPS = async function() {
  if (!navigator.geolocation) {
    alert("Geolocation is not supported by your browser");
    return;
  }

  const gpsBtn = event.currentTarget;
  const originalHTML = gpsBtn.innerHTML;
  gpsBtn.innerHTML = '<ion-icon name="sync-outline" class="animate-spin text-lg"></ion-icon> Locating...';

  navigator.geolocation.getCurrentPosition(
    (position) => {
      const { latitude, longitude } = position.coords;
      addressMap.setView([latitude, longitude], 16);
      addressMarker.setLatLng([latitude, longitude]);
      reverseGeocode(latitude, longitude);
      gpsBtn.innerHTML = originalHTML;
    },
    (error) => {
      alert("GPS positioning failed. Please type address manually.");
      gpsBtn.innerHTML = originalHTML;
    },
    { enableHighAccuracy: true, timeout: 10000 }
  );
};

async function reverseGeocode(lat, lng) {
  try {
    const response = await fetch(`https://nominatim.openstreetmap.org/reverse?format=jsonv2&lat=${lat}&lon=${lng}`);
    const data = await response.json();
    if (data.address) {
      const street = data.address.road || data.address.suburb || data.address.neighbourhood || '';
      const city = data.address.city || data.address.town || data.address.village || '';
      document.getElementById('manual-street').value = street;
      document.getElementById('manual-city').value = city;
    }
  } catch (err) {
    console.error("Geocoding failed", err);
  }
}

window.saveAddress = function() {
  const street = document.getElementById('manual-street').value;
  const city = document.getElementById('manual-city').value;

  if (!street || !city) {
    alert("Please enter both street and city.");
    return;
  }

  const fullAddress = `${street}, ${city}`;
  localStorage.setItem('deliveryAddress', fullAddress);
  document.getElementById('current-address').textContent = fullAddress;
  
  const confirmBtn = event.currentTarget;
  confirmBtn.innerHTML = 'Address Saved!';
  confirmBtn.classList.replace('bg-[#5D4037]', 'bg-green-600');
  
  setTimeout(() => {
    closeAddressModal();
    setTimeout(() => {
      confirmBtn.innerHTML = 'Confirm Delivery Location';
      confirmBtn.classList.replace('bg-green-600', 'bg-[#5D4037]');
    }, 1000);
  }, 800);
};

// DOM Initialization
document.addEventListener('DOMContentLoaded', () => {
  renderProductDetail();
  renderRelatedProducts();
  initHoverZoom();
  initLightbox();

  // Load saved address
  const savedAddress = localStorage.getItem('deliveryAddress');
  if (savedAddress) {
    const addrEl = document.getElementById('current-address');
    if (addrEl) addrEl.textContent = savedAddress;
  }
});

// Dynamic Related Products Loader
function renderRelatedProducts() {
  const container = document.getElementById('related-products-container');
  if (!container) return;

  const currentFilename = window.location.pathname.split('/').pop() || 'Portronics-Highlighter-Set.html';
  const currentSlug = currentFilename.replace('.html', '');
  
  // Filter other products from STUDY_DATA
  const otherKeys = Object.keys(STUDY_DATA).filter(k => k !== currentSlug);
  
  // Slice down to 4 items
  const relatedKeys = otherKeys.slice(0, 4);

  container.innerHTML = relatedKeys.map(key => {
    const item = STUDY_DATA[key];
    return `
      <a href="${key}.html" class="group flex flex-col bg-white rounded-2xl p-4 border border-[#D7CCC8]/30 shadow-premium hover:shadow-xl hover:-translate-y-1 transition-all duration-300">
        <div class="aspect-[3/4] rounded-xl overflow-hidden mb-4 bg-gray-50 relative flex items-center justify-center">
          <img src="${item.images[0]}" class="max-w-full max-h-full object-contain group-hover:scale-105 transition-transform duration-500" alt="${item.title}">
          <div class="absolute top-2 right-2 bg-green-700 text-white text-[10px] font-black px-1.5 py-0.5 rounded flex items-center gap-0.5 z-10">
            <span>${item.rating}</span>
            <ion-icon name="star" class="text-[8px]"></ion-icon>
          </div>
        </div>
        <p class="text-[10px] font-black uppercase tracking-wider text-gray-400 mb-1">${item.brand}</p>
        <h3 class="font-bold text-xs text-[#3E2723] line-clamp-1 group-hover:text-amber-700 transition-colors mb-2">${item.title}</h3>
        <div class="flex items-baseline gap-2 mt-auto">
          <span class="text-sm font-black text-[#3E2723]">₹${item.price}</span>
          <span class="text-[10px] text-gray-400 line-through">₹${item.originalPrice}</span>
          <span class="text-[10px] font-bold text-green-600">${item.discount}</span>
        </div>
      </a>
    `;
  }).join('');
}
