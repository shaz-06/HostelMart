const DEALS_DATA = {
  "Samsung-Galaxy-Z-Fold5": {
    brand: "Samsung",
    title: "Samsung Galaxy Z Fold5 (Icy Blue, 512GB)",
    rating: "4.5",
    ratingsCount: "641 ratings",
    price: 164999,
    originalPrice: 169999,
    discount: "3% OFF",
    images: [
      "https://images.unsplash.com/photo-1691435431627-889816503c20?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1695048133142-1a20484d2569?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1610945265064-0e34e5519bbf?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1511707171634-5f897ff02aa9?auto=format&fit=crop&w=800&q=80"
    ],
    colorOptions: [
      { name: "Icy Blue", img: "https://images.unsplash.com/photo-1691435431627-889816503c20?auto=format&fit=crop&w=800&q=80" },
      { name: "Phantom Black", img: "https://images.unsplash.com/photo-1610945265064-0e34e5519bbf?auto=format&fit=crop&w=800&q=80" },
      { name: "Cream", img: "https://images.unsplash.com/photo-1511707171634-5f897ff02aa9?auto=format&fit=crop&w=800&q=80" }
    ],
    sizes: ["256GB Storage", "512GB Storage"],
    sizeLabel: "Select Capacity Variant",
    highlights: {
      "Brand": "Samsung",
      "Model": "Galaxy Z Fold5",
      "Main Display": "7.6-inch Dynamic AMOLED 2X",
      "Cover Display": "6.2-inch Dynamic AMOLED 2X",
      "Processor": "Snapdragon 8 Gen 2 for Galaxy",
      "Water Protection": "IPX8 Waterproof Rating",
      "Battery Capacity": "4400 mAh Dual Battery"
    },
    extraSections: {
      "Ultimate Multitasking Experience": "With a expansive 7.6-inch screen, run up to three windows simultaneously. Drag and drop content between apps with the advanced taskbar.",
      "Next-Generation S-Pen Support": "Take hand-written lecture notes, sketch designs, or sign student forms directly on the ultra-thin flexible display glass with perfect precision."
    },
    seller: "Samsung Official Store"
  },
  "Samsung-Galaxy-Z-Flip6": {
    brand: "Samsung",
    title: "Samsung Galaxy Z Flip6 5G (Blue, 256GB)",
    rating: "4.1",
    ratingsCount: "230 ratings",
    price: 109840,
    originalPrice: 109999,
    discount: "0% OFF",
    images: [
      "https://images.unsplash.com/photo-1721332150382-d4114ee27cef?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1695048133142-1a20484d2569?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1610945265064-0e34e5519bbf?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1511707171634-5f897ff02aa9?auto=format&fit=crop&w=800&q=80"
    ],
    colorOptions: [
      { name: "Blue", img: "https://images.unsplash.com/photo-1721332150382-d4114ee27cef?auto=format&fit=crop&w=800&q=80" },
      { name: "Mint", img: "https://images.unsplash.com/photo-1695048133142-1a20484d2569?auto=format&fit=crop&w=800&q=80" }
    ],
    sizes: ["256GB Storage", "512GB Storage"],
    sizeLabel: "Select Capacity Variant",
    highlights: {
      "Brand": "Samsung",
      "Model": "Galaxy Z Flip6",
      "Main Display": "6.7-inch Dynamic AMOLED 2X",
      "Flex Display": "3.4-inch Super AMOLED",
      "Processor": "Snapdragon 8 Gen 3 for Galaxy",
      "Rear Camera": "50MP Dual FlexCam with Auto-Zoom",
      "Protection Rating": "IP48 Waterproof & Dust Resistant"
    },
    extraSections: {
      "Compact Folding Innovation": "Folds down ultra compact to fit neatly into pockets, clutches, or small backpacks. Personalize your front cover screen with customized widgets.",
      "Hands-Free FlexMode Photos": "Prop the phone open on any flat surface to take perfect group selfies, time-lapse videos, or stream video content completely hands-free."
    },
    seller: "Samsung Official Store"
  },
  "Samsung-Galaxy-Z-Flip3": {
    brand: "Samsung",
    title: "Samsung Galaxy Z Flip3 5G (Cream, 128GB)",
    rating: "4.2",
    ratingsCount: "9,650 ratings",
    price: 95999,
    originalPrice: 95999,
    discount: "Flat Deal",
    images: [
      "https://images.unsplash.com/photo-1695048133142-1a20484d2569?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1610945265064-0e34e5519bbf?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1511707171634-5f897ff02aa9?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1721332150382-d4114ee27cef?auto=format&fit=crop&w=800&q=80"
    ],
    colorOptions: [
      { name: "Cream", img: "https://images.unsplash.com/photo-1695048133142-1a20484d2569?auto=format&fit=crop&w=800&q=80" },
      { name: "Phantom Black", img: "https://images.unsplash.com/photo-1610945265064-0e34e5519bbf?auto=format&fit=crop&w=800&q=80" }
    ],
    sizes: ["128GB Storage", "256GB Storage"],
    sizeLabel: "Select Capacity Variant",
    highlights: {
      "Brand": "Samsung",
      "Model": "Galaxy Z Flip3 5G",
      "Main Display": "6.7-inch Dynamic AMOLED 120Hz",
      "Water Resistant": "IPX8 Certified Water Resistant",
      "Processor": "Snapdragon 888 5G",
      "Armor Frame": "Super Strong Armor Aluminum Frame",
      "Rear Camera": "12MP Ultra-Wide Dual Lens"
    },
    extraSections: {
      "Iconic Folding Flip Style": "A modern flip phone combining old-school nostalgia with flagship performance. Slip it easily in the tightest pockets.",
      "Hands-Free Flex Cam": "Take high-fidelity selfies from the rear camera using the convenient external cover screen as a real-time preview."
    },
    seller: "Samsung Official Retailer"
  },
  "Motul-7100": {
    brand: "MOTUL",
    title: "MOTUL 7100 4T 20W-50 Synthetic Ester Engine Oil",
    rating: "4.7",
    ratingsCount: "380 ratings",
    price: 1422,
    originalPrice: 1517,
    discount: "6% OFF",
    images: [
      "https://images.unsplash.com/photo-1620916566398-39f1143ab7be?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1619642751034-765dfdf7c58e?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1607603750909-408e19385117?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1580273916550-e323be2ae537?auto=format&fit=crop&w=800&q=80"
    ],
    colorOptions: [
      { name: "Synthetic Ester Red", img: "https://images.unsplash.com/photo-1620916566398-39f1143ab7be?auto=format&fit=crop&w=800&q=80" }
    ],
    sizes: ["1.25L Bottle", "2.5L Double-Pack", "5L Garage-Pack"],
    sizeLabel: "Select Pack Volume",
    highlights: {
      "Brand": "MOTUL",
      "Viscosity": "SAE 20W50 Grade",
      "Oil Type": "100% Synthetic with Ester Core Technology",
      "Standards": "JASO MA2 / API SN Certified",
      "Thermal Stability": "High Shear Resistance",
      "Clutch Friction": "Optimal Wet Clutch Performance",
      "Engine Durability": "Advanced Anti-Wear Formulations"
    },
    extraSections: {
      "100% Synthetic Ester Tech": "Exclusively engineered Ester Core technology reduces internal engine friction while offering absolute protection at high operational temperatures.",
      "Smooth Gear Shifts": "Specially formulated clutch enhancers facilitate smoother, crisper gear changes and wet clutch operations under all driving styles."
    },
    seller: "MOTUL Distributors India"
  },
  "Good-Friend-Cap": {
    brand: "Good Friend",
    title: "Good Friend Sports/Regular Adjustable Cap",
    rating: "4.3",
    ratingsCount: "192 ratings",
    price: 153,
    originalPrice: 999,
    discount: "84% OFF",
    images: [
      "https://images.unsplash.com/photo-1572307480813-ceb0e59d8325?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1588850561407-ed78c282e1c7?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1534215754734-18e55d13ce35?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1521572163474-6864f9cf17ab?auto=format&fit=crop&w=800&q=80"
    ],
    colorOptions: [
      { name: "Navy Blue", img: "https://images.unsplash.com/photo-1572307480813-ceb0e59d8325?auto=format&fit=crop&w=800&q=80" },
      { name: "Classic Black", img: "https://images.unsplash.com/photo-1588850561407-ed78c282e1c7?auto=format&fit=crop&w=800&q=80" }
    ],
    sizes: ["Adjustable Strap", "FlexFit Closed Back"],
    sizeLabel: "Select Size/Fit",
    highlights: {
      "Brand": "Good Friend",
      "Material": "100% Breathable Combed Cotton",
      "Visor Type": "Pre-Curved Sport Visor",
      "Back Closure": "Premium Metal Buckle Slider",
      "Sweatband": "Built-in Sweat-Absorbing Band",
      "Usage": "Sports, Travel, Everyday Sunwear"
    },
    extraSections: {
      "Everyday Breathable Comfort": "Crafted from fine twill cotton that permits maximum ventilation, keeping your head cool during long summer commutes on campus.",
      "Unisex Ergonomic Sizing": "Features an adjustable brass sliding strap at the back to fit a wide range of head sizes comfortably without slipping."
    },
    seller: "Good Friend Apparel Store"
  },
  "Lexrex-Loose-Fit": {
    brand: "LEXREX",
    title: "LEXREX Men Loose Fit Premium Trousers",
    rating: "4.2",
    ratingsCount: "105 ratings",
    price: 351,
    originalPrice: 999,
    discount: "64% OFF",
    images: [
      "https://images.unsplash.com/photo-1594633312681-425c7b97ccd1?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1624378439575-d8705ad7ae80?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1479064555552-3ef4979f8908?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1516257984-b1b4d707412e?auto=format&fit=crop&w=800&q=80"
    ],
    colorOptions: [
      { name: "Black Cotton", img: "https://images.unsplash.com/photo-1594633312681-425c7b97ccd1?auto=format&fit=crop&w=800&q=80" },
      { name: "Olive Green", img: "https://images.unsplash.com/photo-1624378439575-d8705ad7ae80?auto=format&fit=crop&w=800&q=80" }
    ],
    sizes: ["30 Waist", "32 Waist", "34 Waist", "36 Waist"],
    sizeLabel: "Select Size",
    highlights: {
      "Brand": "LEXREX",
      "Fit Style": "Modern Loose Fit Silhouette",
      "Material": "Premium Heavyweight Cotton Twill",
      "Pockets Count": "4 Deep Utility Pockets",
      "Waistband": "Semi-Elasticated Flex Band",
      "Wash Care": "Machine wash cold, gentle cycle"
    },
    extraSections: {
      "Relaxed Streetwear Comfort": "Designed for maximum room and effortless styling. The loose tailoring provides freedom of movement, perfect for lounging or studying.",
      "Heavyweight Durable Fabric": "Built with ultra-durable twill cotton that retains its premium structure, shape, and color finish even after repeated washes."
    },
    seller: "LEXREX Denim Hub"
  },
  "Eriko-Sporty-Polo": {
    brand: "Eriko",
    title: "Eriko Men Sporty Polo Neck T-Shirt",
    rating: "4.1",
    ratingsCount: "94 ratings",
    price: 342,
    originalPrice: 1999,
    discount: "82% OFF",
    images: [
      "https://images.unsplash.com/photo-1586363104862-3a5e2ab60d99?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1521572163474-6864f9cf17ab?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1572307480813-ceb0e59d8325?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1588850561407-ed78c282e1c7?auto=format&fit=crop&w=800&q=80"
    ],
    colorOptions: [
      { name: "Textured Red", img: "https://images.unsplash.com/photo-1586363104862-3a5e2ab60d99?auto=format&fit=crop&w=800&q=80" },
      { name: "Midnight Black", img: "https://images.unsplash.com/photo-1521572163474-6864f9cf17ab?auto=format&fit=crop&w=800&q=80" }
    ],
    sizes: ["S", "M", "L", "XL"],
    sizeLabel: "Select Size",
    highlights: {
      "Brand": "Eriko",
      "Neckline Style": "Sporty Ribbed Polo Collar",
      "Fabric blend": "Polyester Cotton Athletic Blend",
      "Moisture Wicking": "Yes (Quick Dry Technology)",
      "Fit Style": "Ergonomic Regular Fit",
      "Logo Detail": "Subtle Athletic Side Chest Print"
    },
    extraSections: {
      "Moisture-Wicking Action": "Advanced sweat-dispelling fabric transfers skin moisture rapidly to the surface, drying quickly to keep you comfortable during active sports.",
      "Structured Ribbed Collar": "Specially engineered collar stays clean and retains its crisp shape without curling or folding after machine cycles."
    },
    seller: "Eriko Athleisure Store"
  },
  "Angar-Look-Cap": {
    brand: "Angar Look",
    title: "Angar Look Self Design Sports Curved Cap",
    rating: "4.4",
    ratingsCount: "133 ratings",
    price: 191,
    originalPrice: 699,
    discount: "72% OFF",
    images: [
      "https://images.unsplash.com/photo-1588850561407-ed78c282e1c7?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1572307480813-ceb0e59d8325?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1534215754734-18e55d13ce35?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1521572163474-6864f9cf17ab?auto=format&fit=crop&w=800&q=80"
    ],
    colorOptions: [
      { name: "Classic Black", img: "https://images.unsplash.com/photo-1588850561407-ed78c282e1c7?auto=format&fit=crop&w=800&q=80" },
      { name: "Royal Blue", img: "https://images.unsplash.com/photo-1572307480813-ceb0e59d8325?auto=format&fit=crop&w=800&q=80" }
    ],
    sizes: ["One Size Fits All"],
    sizeLabel: "Select Size",
    highlights: {
      "Brand": "Angar Look",
      "Material": "Stiffened Cotton Poly Blend",
      "Design": "Premium Self-Design Curved Visor",
      "Closure": "Adjustable Velcro Strap",
      "Ventilation": "Metal Eyelets for Airflow",
      "Crown Profile": "Structured 6-Panel High Crown"
    },
    extraSections: {
      "Sports Structural Fit": "Features a stiffened front crown lining that maintains its high-profile athletic shape even when stored or packed tightly.",
      "Maximum Shielding Curved Visor": "A perfectly curved visor designed to offer maximum protection from harsh sun rays during outdoor training or camp strolls."
    },
    seller: "Angar Look Headwear Outlet"
  },
  "Else-What-Tshirt": {
    brand: "Else & What",
    title: "Else & What Men Printed Round Neck T-Shirt",
    rating: "4.2",
    ratingsCount: "148 ratings",
    price: 373,
    originalPrice: 999,
    discount: "62% OFF",
    images: [
      "https://images.unsplash.com/photo-1521572163474-6864f9cf17ab?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1586363104862-3a5e2ab60d99?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1572307480813-ceb0e59d8325?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1588850561407-ed78c282e1c7?auto=format&fit=crop&w=800&q=80"
    ],
    colorOptions: [
      { name: "Graphic White", img: "https://images.unsplash.com/photo-1521572163474-6864f9cf17ab?auto=format&fit=crop&w=800&q=80" }
    ],
    sizes: ["S", "M", "L", "XL"],
    sizeLabel: "Select Size",
    highlights: {
      "Brand": "Else & What",
      "Material": "100% Super-Combed Cotton",
      "GSM Profile": "Premium 180 GSM Fabric",
      "Neck Style": "Comfortable Ribbed Crew Neck",
      "Print Tech": "Wash-Resistant High-Grade Screen Print",
      "Fit Style": "Youthful Regular Fit"
    },
    extraSections: {
      "Premium Screen Printed Graphics": "Features a high-resolution, elastic ink graphic print that will not crack, peel, or fade over multiple hot wash cycles.",
      "Ultra Soft Breathable Cotton": "Woven with premium 100% super-combed cotton fibers offering superior handfeel and coolness under summer weather."
    },
    seller: "Else & What Apparel Co."
  },
  "Mexican-Lubricant": {
    brand: "Mexican",
    title: "Mexican MX-10W30-SN-FS Premium Lubricant",
    rating: "4.3",
    ratingsCount: "74 ratings",
    price: 223,
    originalPrice: 558,
    discount: "60% OFF",
    images: [
      "https://images.unsplash.com/photo-1486006920555-c77dcf18193c?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1620916566398-39f1143ab7be?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1619642751034-765dfdf7c58e?auto=format&fit=crop&w=800&q=80",
      "https://images.unsplash.com/photo-1580273916550-e323be2ae537?auto=format&fit=crop&w=800&q=80"
    ],
    colorOptions: [
      { name: "Standard 1L Can", img: "https://images.unsplash.com/photo-1486006920555-c77dcf18193c?auto=format&fit=crop&w=800&q=80" }
    ],
    sizes: ["1L Can", "2L Multi-Pack"],
    sizeLabel: "Select Pack Volume",
    highlights: {
      "Brand": "Mexican",
      "Viscosity Grade": "SAE 10W30 API SN",
      "Formulation": "Fully Synthetic (FS) High-performance Engine Oil",
      "Engine Care": "Advanced Sludge & Oxidation Prevention",
      "Viscosity Index": "JASO MA2 Certified Friction Coefficient",
      "Application": "Ideal for 4-Stroke Commuter Bikes & Scooters"
    },
    extraSections: {
      "Advanced Sludge Prevention": "Active cleaning agents sweep away carbon sediments, oil breakdown residues, and deposit sludge from moving internal parts.",
      "Smooth Low Friction Commutes": "Significantly decreases operational friction drag, increasing overall mileage and ensuring silent engine operations under stress."
    },
    seller: "Mexican PetroChemicals Direct"
  }
};

function getActiveProduct() {
  const filename = window.location.pathname.split('/').pop() || 'Samsung-Galaxy-Z-Fold5.html';
  const slug = filename.replace('.html', '');
  return DEALS_DATA[slug] || DEALS_DATA["Samsung-Galaxy-Z-Fold5"];
}

// Global active selection states
let selectedColor = "";
let selectedSize = "";
let currentAction = null;

function renderProductDetail() {
  const prod = getActiveProduct();
  if (!prod) return;

  // 1. Update document head title
  document.title = `${prod.title} | HostelMart`;

  // 2. Breadcrumbs Setup
  const navEl = document.querySelector('main > nav');
  if (navEl) {
    navEl.innerHTML = `
      <a href="index.html" class="hover:text-[#3E2723]">Home</a>
      <ion-icon name="chevron-forward-outline"></ion-icon>
      <a href="index.html#top-deals" class="hover:text-[#3E2723]">Top Deals</a>
      <ion-icon name="chevron-forward-outline"></ion-icon>
      <a href="javascript:void(0)" class="hover:text-[#3E2723]">${prod.brand}</a>
      <ion-icon name="chevron-forward-outline"></ion-icon>
      <span class="text-[#3E2723] font-bold">${prod.title}</span>
    `;
  }

  // 3. Grid Images Setup
  for (let i = 0; i < 4; i++) {
    const imgEl = document.getElementById(`main-img-${i + 1}`);
    if (imgEl) {
      imgEl.src = prod.images[i] || prod.images[0];
      imgEl.alt = `${prod.title} View ${i + 1}`;
    }
  }

  // 4. Product Details Headers
  const brandEl = document.getElementById('product-brand');
  const titleEl = document.getElementById('product-title');
  if (brandEl) brandEl.textContent = prod.brand;
  if (titleEl) titleEl.textContent = prod.title;

  // 5. Ratings & Reviews
  const ratingEl = document.getElementById('product-rating');
  const ratingCountEl = document.getElementById('product-rating-count');
  if (ratingEl) ratingEl.textContent = prod.rating;
  if (ratingCountEl) ratingCountEl.textContent = prod.ratingsCount;

  // 6. Pricing Section
  const priceEl = document.getElementById('product-price');
  const originalPriceEl = document.getElementById('product-original-price');
  const discountEl = document.getElementById('product-discount');
  const buyBtnText = document.getElementById('buy-btn-price-text');
  
  if (priceEl) priceEl.textContent = `₹${prod.price.toLocaleString('en-IN')}`;
  if (originalPriceEl) originalPriceEl.textContent = `₹${prod.originalPrice.toLocaleString('en-IN')}`;
  if (discountEl) discountEl.textContent = prod.discount;
  if (buyBtnText) buyBtnText.textContent = `Buy at ₹${prod.price.toLocaleString('en-IN')}`;

  const dealEl = document.getElementById('deal-saving-text');
  if (dealEl) {
    const dealPrice = Math.floor(prod.price * 0.95); // 5% off for WOW Deal
    dealEl.textContent = `Buy at ₹${dealPrice.toLocaleString('en-IN')}`;
  }

  // Update size label in UI
  const labelEl = document.querySelector('p.text-\\[10px\\].font-bold.text-gray-400.uppercase.tracking-widest:nth-of-type(2)');
  if (labelEl && prod.sizeLabel) {
    labelEl.textContent = prod.sizeLabel;
  }

  // 7. Variants Color Builder
  const variantsContainer = document.getElementById('variants-container');
  if (variantsContainer && prod.colorOptions) {
    selectedColor = prod.colorOptions[0].name;
    document.getElementById('color-label').textContent = selectedColor;

    variantsContainer.innerHTML = prod.colorOptions.map((opt, idx) => `
      <button onclick="updateVariant('${opt.name}')" class="variant-btn ${idx === 0 ? 'active' : ''} w-16 h-20 rounded-xl overflow-hidden border-2 border-transparent transition-all shadow-md">
        <img src="${opt.img}" class="w-full h-full object-cover">
      </button>
    `).join('');
  }

  // 8. Size Selector chips Builder
  const sizesContainer = document.getElementById('sizes-container');
  if (sizesContainer && prod.sizes) {
    sizesContainer.innerHTML = prod.sizes.map(sz => `
      <button onclick="selectSize(this)" class="size-btn px-4 h-12 rounded-xl border border-[#D7CCC8] flex items-center justify-center text-xs font-black transition-all hover:border-[#3E2723]">${sz}</button>
    `).join('');
  }

  // 9. Highlights Specifications Builder
  const highlightsContainer = document.getElementById('highlights-container');
  if (highlightsContainer && prod.highlights) {
    highlightsContainer.innerHTML = Object.entries(prod.highlights).map(([key, val]) => `
      <div class="space-y-1">
        <p class="text-[10px] font-bold text-gray-400 uppercase tracking-widest">${key}</p>
        <p class="text-xs font-black">${val}</p>
      </div>
    `).join('');
  }

  // 9.1 Dynamic Extra Sections Builder
  const extraContainer = document.getElementById('extra-sections-container');
  if (extraContainer) {
    if (prod.extraSections) {
      extraContainer.classList.remove('hidden');
      extraContainer.innerHTML = Object.entries(prod.extraSections).map(([key, val]) => `
        <div class="space-y-2 pt-6 border-t border-[#D7CCC8]/30">
          <p class="text-[10px] font-bold text-gray-400 uppercase tracking-widest">${key}</p>
          <p class="text-xs font-semibold leading-relaxed text-gray-600">${val}</p>
        </div>
      `).join('');
    } else {
      extraContainer.classList.add('hidden');
      extraContainer.innerHTML = "";
    }
  }

  // 10. Seller & Return Details
  const sellerEl = document.getElementById('seller-name');
  if (sellerEl) sellerEl.textContent = prod.seller;

  // 11. Select Variant Modal Sync
  const modalImg = document.querySelector('.variant-modal-content img');
  const modalBrand = document.querySelector('.variant-modal-content h4');
  const modalTitle = document.querySelector('.variant-modal-content p');
  const modalOriginal = document.querySelector('.variant-modal-content span.text-xs');
  const modalPrice = document.querySelector('.variant-modal-content span.text-lg');
  const modalSizesContainer = document.querySelector('.variant-modal-content .justify-between');

  if (modalImg) modalImg.src = prod.images[0];
  if (modalBrand) modalBrand.textContent = prod.brand;
  if (modalTitle) modalTitle.textContent = prod.title;
  if (modalOriginal) modalOriginal.textContent = `₹${prod.originalPrice.toLocaleString('en-IN')}`;
  if (modalPrice) modalPrice.textContent = `₹${prod.price.toLocaleString('en-IN')}`;
  
  if (modalSizesContainer && prod.sizes) {
    modalSizesContainer.innerHTML = prod.sizes.map(sz => `
      <button onclick="selectSizeModal(this)" class="size-btn-modal px-3 py-2 rounded-xl border border-gray-200 flex items-center justify-center text-xs font-black transition-all hover:border-[#3E2723]">${sz}</button>
    `).join('');
  }

  // Dynamic Date calculation (+2 days for superfast express student shipping)
  const deliveryDate = new Date();
  deliveryDate.setDate(deliveryDate.getDate() + 2);
  const dateOptions = { day: 'numeric', month: 'short', weekday: 'short' };
  const deliveryDateStr = deliveryDate.toLocaleDateString('en-IN', dateOptions);
  
  const deliveryDateEl = document.getElementById('delivery-date-estimate');
  if (deliveryDateEl) deliveryDateEl.textContent = deliveryDateStr;

  // Sync added state
  const filename = window.location.pathname.split('/').pop() || 'Samsung-Galaxy-Z-Fold5.html';
  const slug = filename.replace('.html', '');
  const cart = JSON.parse(localStorage.getItem('hostelmart_cart')) || [];
  const inCart = cart.some(item => item.slug === slug || item.id === slug);
  if (inCart) {
    updateCartButtonState();
  }
}

// User Action Handlers
window.selectSize = function(btn) {
  document.querySelectorAll('.size-btn').forEach(b => b.classList.remove('active', 'bg-[#3E2723]', 'text-white'));
  btn.classList.add('active', 'bg-[#3E2723]', 'text-white');
  selectedSize = btn.innerText;
};

window.updateVariant = function(color) {
  document.querySelectorAll('.variant-btn').forEach(b => b.classList.remove('active'));
  event.currentTarget.classList.add('active');
  selectedColor = color;
  document.getElementById('color-label').innerText = color;
  
  // Swap the main preview image based on variant
  const prod = getActiveProduct();
  const matched = prod.colorOptions.find(o => o.name === color);
  if (matched) {
    const mainImg = document.getElementById('main-img-1');
    if (mainImg) mainImg.src = matched.img;
  }
};

window.handleBuyNow = function() {
  if (!selectedSize) {
    currentAction = 'buy';
    openVariantModal();
  } else {
    const prod = getActiveProduct();
    const filename = window.location.pathname.split('/').pop() || 'Samsung-Galaxy-Z-Fold5.html';
    const slug = filename.replace('.html', '');
    const data = {
      id: slug,
      slug: slug,
      img: prod.images[0],
      brand: prod.brand,
      title: prod.title,
      currentPrice: `₹${prod.price}`,
      originalPrice: `₹${prod.originalPrice}`,
      size: selectedSize,
      color: selectedColor,
      qty: 1
    };
    localStorage.setItem('checkoutItem', JSON.stringify(data));
    window.location.href = 'Checkout.html';
  }
};

window.handleAddToCart = function() {
  if (!selectedSize) {
    currentAction = 'cart';
    openVariantModal();
  } else {
    const prod = getActiveProduct();
    const filename = window.location.pathname.split('/').pop() || 'Samsung-Galaxy-Z-Fold5.html';
    const slug = filename.replace('.html', '');
    const data = {
      id: slug,
      slug: slug,
      img: prod.images[0],
      brand: prod.brand,
      title: prod.title,
      currentPrice: `₹${prod.price}`,
      originalPrice: `₹${prod.originalPrice}`,
      size: selectedSize,
      color: selectedColor,
      qty: 1
    };
    if (window.addToCart) {
      window.addToCart(data);
    }
    updateCartButtonState();
  }
};

window.confirmVariantSelection = function() {
  const selectedSizeModal = document.querySelector('.size-btn-modal.active');
  if (!selectedSizeModal) return;
  
  selectedSize = selectedSizeModal.innerText;
  
  // Reflect size on main selectors
  document.querySelectorAll('.size-btn').forEach(b => {
    if (b.innerText === selectedSize) {
      b.classList.add('active', 'bg-[#3E2723]', 'text-white');
    } else {
      b.classList.remove('active', 'bg-[#3E2723]', 'text-white');
    }
  });

  const prod = getActiveProduct();
  const filename = window.location.pathname.split('/').pop() || 'Samsung-Galaxy-Z-Fold5.html';
  const slug = filename.replace('.html', '');
  const data = {
    id: slug,
    slug: slug,
    img: prod.images[0],
    brand: prod.brand,
    title: prod.title,
    currentPrice: `₹${prod.price}`,
    originalPrice: `₹${prod.originalPrice}`,
    size: selectedSize,
    color: selectedColor,
    qty: 1
  };

  if (currentAction === 'buy') {
    localStorage.setItem('checkoutItem', JSON.stringify(data));
    window.location.href = 'Checkout.html';
  } else {
    if (window.addToCart) {
      window.addToCart(data);
    }
    updateCartButtonState();
    closeVariantModal();
  }
};

window.selectSizeModal = function(btn) {
  document.querySelectorAll('.size-btn-modal').forEach(b => b.classList.remove('active', 'bg-[#3E2723]', 'text-white'));
  btn.classList.add('active', 'bg-[#3E2723]', 'text-white');
  document.getElementById('modal-continue-btn').disabled = false;
};

window.updateCartButtonState = function() {
  const mainBtn = document.getElementById('add-to-cart-btn');
  if (mainBtn) {
    mainBtn.innerHTML = '<ion-icon name="bag-handle-outline" class="text-lg"></ion-icon> Go To Cart';
    mainBtn.onclick = () => window.location.href = 'Cart.html';
    mainBtn.classList.replace('bg-white', 'bg-[#FDFBF7]');
  }
};

window.openVariantModal = function() {
  const modal = document.getElementById('variant-modal');
  modal.classList.remove('hidden');
  document.body.style.overflow = 'hidden';
  setTimeout(() => {
    modal.classList.add('active');
  }, 10);
};

window.closeVariantModal = function() {
  const modal = document.getElementById('variant-modal');
  modal.classList.remove('active');
  setTimeout(() => {
    modal.classList.add('hidden');
    document.body.style.overflow = 'auto';
  }, 300);
};

// Premium Features: Hover Zoom Tracking
function initHoverZoom() {
  document.querySelectorAll('.image-zoom-container').forEach(container => {
    const img = container.querySelector('img');
    if (!img) return;

    container.addEventListener('mousemove', e => {
      const rect = container.getBoundingClientRect();
      const x = ((e.clientX - rect.left) / rect.width) * 100;
      const y = ((e.clientY - rect.top) / rect.height) * 100;
      img.style.transformOrigin = `${x}% ${y}%`;
      img.style.transform = 'scale(1.5)';
    });

    container.addEventListener('mouseleave', () => {
      img.style.transform = 'scale(1)';
      img.style.transformOrigin = 'center center';
    });
  });
}

// Lightbox modal with thumbnail switcher
function initLightbox() {
  const lightboxHTML = `
    <div id="lightbox-modal" class="fixed inset-0 z-[150] bg-black/95 backdrop-blur-md hidden flex items-center justify-center p-4">
      <button id="close-lightbox" class="absolute top-6 right-6 text-white text-3xl p-3 hover:bg-white/10 rounded-full transition-all">
        <ion-icon name="close-outline"></ion-icon>
      </button>
      
      <div class="flex flex-col md:flex-row items-center gap-8 max-w-5xl w-full h-[80vh] md:h-auto">
        <div class="flex md:flex-col gap-3 overflow-x-auto md:overflow-x-visible no-scrollbar max-w-full md:max-w-none flex-shrink-0" id="lightbox-thumbs">
        </div>
        <div class="flex-1 h-[50vh] md:h-[70vh] rounded-3xl overflow-hidden bg-black/40 border border-white/10 relative flex items-center justify-center">
          <img id="lightbox-main-img" src="" class="max-w-full max-h-full object-contain transition-all duration-300">
        </div>
      </div>
    </div>
  `;
  document.body.insertAdjacentHTML('beforeend', lightboxHTML);

  const modal = document.getElementById('lightbox-modal');
  const mainImg = document.getElementById('lightbox-main-img');
  const thumbsContainer = document.getElementById('lightbox-thumbs');
  const closeBtn = document.getElementById('close-lightbox');

  closeBtn.onclick = () => {
    modal.classList.add('hidden');
    document.body.style.overflow = 'auto';
  };

  modal.onclick = (e) => {
    if (e.target === modal) {
      modal.classList.add('hidden');
      document.body.style.overflow = 'auto';
    }
  };

  document.querySelectorAll('.image-zoom-container').forEach((gridContainer, idx) => {
    gridContainer.style.cursor = 'zoom-in';
    gridContainer.onclick = () => {
      const activeProd = getActiveProduct();
      if (!activeProd) return;
      
      thumbsContainer.innerHTML = activeProd.images.map((imgUrl, i) => `
        <div class="w-16 h-20 rounded-xl overflow-hidden cursor-pointer border-2 ${i === idx ? 'border-amber-400' : 'border-transparent'} transition-all shadow-md lightbox-thumb-item" onclick="switchLightboxImage(${i})">
          <img src="${imgUrl}" class="w-full h-full object-cover">
        </div>
      `).join('');
      
      mainImg.src = activeProd.images[idx];
      modal.classList.remove('hidden');
      document.body.style.overflow = 'hidden';
    };
  });
}

window.switchLightboxImage = function(idx) {
  const activeProd = getActiveProduct();
  if (!activeProd) return;
  
  const mainImg = document.getElementById('lightbox-main-img');
  mainImg.src = activeProd.images[idx];
  
  document.querySelectorAll('.lightbox-thumb-item').forEach((item, i) => {
    if (i === idx) {
      item.classList.add('border-amber-400');
      item.classList.remove('border-transparent');
    } else {
      item.classList.add('border-transparent');
      item.classList.remove('border-amber-400');
    }
  });
};

// Map GPS Address Modal Logic
let addressMap;
let addressMarker;

window.openAddressModal = function() {
  const modal = document.getElementById('address-modal');
  const container = document.getElementById('address-container');
  
  modal.classList.remove('hidden');
  document.body.style.overflow = 'hidden';

  setTimeout(() => {
    container.classList.remove('scale-95', 'opacity-0');
    container.classList.add('scale-100', 'opacity-100');
    initAddressMap();
  }, 10);
};

window.closeAddressModal = function() {
  const container = document.getElementById('address-container');
  
  container.classList.remove('scale-100', 'opacity-100');
  container.classList.add('scale-95', 'opacity-0');

  setTimeout(() => {
    document.getElementById('address-modal').classList.add('hidden');
    document.body.style.overflow = 'auto';
  }, 500);
};

window.initAddressMap = function() {
  if (addressMap) return;

  const defaultLoc = [12.9716, 77.5946];
  addressMap = L.map('address-map').setView(defaultLoc, 13);

  L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
    attribution: '&copy; OpenStreetMap contributors'
  }).addTo(addressMap);

  addressMarker = L.marker(defaultLoc, { draggable: true }).addTo(addressMap);

  addressMarker.on('dragend', function() {
    const position = addressMarker.getLatLng();
    reverseGeocode(position.lat, position.lng);
  });

  addressMap.on('click', function(e) {
    addressMarker.setLatLng(e.latlng);
    reverseGeocode(e.latlng.lat, e.latlng.lng);
  });
};

window.useGPS = async function() {
  if (!navigator.geolocation) {
    alert("Geolocation is not supported by your browser");
    return;
  }

  const gpsBtn = event.currentTarget;
  const originalHTML = gpsBtn.innerHTML;
  gpsBtn.innerHTML = '<ion-icon name="sync-outline" class="animate-spin text-lg"></ion-icon> Locating...';

  navigator.geolocation.getCurrentPosition(
    (position) => {
      const { latitude, longitude } = position.coords;
      addressMap.setView([latitude, longitude], 16);
      addressMarker.setLatLng([latitude, longitude]);
      reverseGeocode(latitude, longitude);
      gpsBtn.innerHTML = originalHTML;
    },
    (error) => {
      alert("GPS positioning failed. Please type address manually.");
      gpsBtn.innerHTML = originalHTML;
    },
    { enableHighAccuracy: true, timeout: 10000 }
  );
};

async function reverseGeocode(lat, lng) {
  try {
    const response = await fetch(`https://nominatim.openstreetmap.org/reverse?format=jsonv2&lat=${lat}&lon=${lng}`);
    const data = await response.json();
    if (data.address) {
      const street = data.address.road || data.address.suburb || data.address.neighbourhood || '';
      const city = data.address.city || data.address.town || data.address.village || '';
      document.getElementById('manual-street').value = street;
      document.getElementById('manual-city').value = city;
    }
  } catch (err) {
    console.error("Geocoding failed", err);
  }
}

window.saveAddress = function() {
  const street = document.getElementById('manual-street').value;
  const city = document.getElementById('manual-city').value;

  if (!street || !city) {
    alert("Please enter both street and city.");
    return;
  }

  const fullAddress = `${street}, ${city}`;
  localStorage.setItem('deliveryAddress', fullAddress);
  document.getElementById('current-address').textContent = fullAddress;
  
  const confirmBtn = event.currentTarget;
  confirmBtn.innerHTML = 'Address Saved!';
  confirmBtn.classList.replace('bg-[#5D4037]', 'bg-green-600');
  
  setTimeout(() => {
    closeAddressModal();
    setTimeout(() => {
      confirmBtn.innerHTML = 'Confirm Delivery Location';
      confirmBtn.classList.replace('bg-green-600', 'bg-[#5D4037]');
    }, 1000);
  }, 800);
};

// DOM Initialization
document.addEventListener('DOMContentLoaded', () => {
  renderProductDetail();
  renderRelatedProducts();
  initHoverZoom();
  initLightbox();

  // Load saved address
  const savedAddress = localStorage.getItem('deliveryAddress');
  if (savedAddress) {
    const addrEl = document.getElementById('current-address');
    if (addrEl) addrEl.textContent = savedAddress;
  }
});

// Dynamic Related Products Loader
function renderRelatedProducts() {
  const container = document.getElementById('related-products-container');
  if (!container) return;

  const currentFilename = window.location.pathname.split('/').pop() || 'Samsung-Galaxy-Z-Fold5.html';
  const currentSlug = currentFilename.replace('.html', '');
  
  // Filter other products from DEALS_DATA
  const otherKeys = Object.keys(DEALS_DATA).filter(k => k !== currentSlug);
  
  // Slice down to 4 items
  const relatedKeys = otherKeys.slice(0, 4);

  container.innerHTML = relatedKeys.map(key => {
    const item = DEALS_DATA[key];
    return `
      <a href="${key}.html" class="group flex flex-col bg-white rounded-2xl p-4 border border-[#D7CCC8]/30 shadow-premium hover:shadow-xl hover:-translate-y-1 transition-all duration-300">
        <div class="aspect-[3/4] rounded-xl overflow-hidden mb-4 bg-gray-50 relative flex items-center justify-center">
          <img src="${item.images[0]}" class="max-w-full max-h-full object-contain group-hover:scale-105 transition-transform duration-500" alt="${item.title}">
          <div class="absolute top-2 right-2 bg-green-700 text-white text-[10px] font-black px-1.5 py-0.5 rounded flex items-center gap-0.5 z-10">
            <span>${item.rating}</span>
            <ion-icon name="star" class="text-[8px]"></ion-icon>
          </div>
        </div>
        <p class="text-[10px] font-black uppercase tracking-wider text-gray-400 mb-1">${item.brand}</p>
        <h3 class="font-bold text-xs text-[#3E2723] line-clamp-1 group-hover:text-amber-700 transition-colors mb-2">${item.title}</h3>
        <div class="flex items-baseline gap-2 mt-auto">
          <span class="text-sm font-black text-[#3E2723]">₹${item.price.toLocaleString('en-IN')}</span>
          <span class="text-[10px] text-gray-400 line-through">₹${item.originalPrice.toLocaleString('en-IN')}</span>
          <span class="text-[10px] font-bold text-green-600">${item.discount}</span>
        </div>
      </a>
    `;
  }).join('');
}
